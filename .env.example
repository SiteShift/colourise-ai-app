# AI Colorizer App - Environment Variables
# Copy this to .env and fill in your values

# ===========================================
# SUPABASE (Required)
# ===========================================
# Get these from your Supabase project settings > API
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ===========================================
# REVENUECAT (Required for payments)
# ===========================================
# Get these from RevenueCat dashboard > API Keys
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=appl_your_ios_key
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=goog_your_android_key

# ===========================================
# REMOVED - Now Server-Side Only
# ===========================================
# The following keys are NO LONGER used in the client app.
# They are now stored as Supabase secrets for Edge Functions.
#
# DO NOT add these to your .env file:
# - EXPO_PUBLIC_DEEPAI_API_KEY (moved to Supabase secrets)
# - EXPO_PUBLIC_OPENAI_API_KEY (moved to Supabase secrets)
# - EXPO_PUBLIC_CLOUDINARY_CLOUD_NAME (moved to Supabase secrets)
# - EXPO_PUBLIC_CLOUDINARY_API_KEY (moved to Supabase secrets)
#
# To set up server-side secrets, run:
# supabase secrets set DEEPAI_API_KEY=xxx
# supabase secrets set OPENAI_API_KEY=xxx
# supabase secrets set CLOUDINARY_CLOUD_NAME=xxx
# supabase secrets set CLOUDINARY_API_KEY=xxx
# supabase secrets set CLOUDINARY_API_SECRET=xxx
# supabase secrets set REVENUECAT_WEBHOOK_AUTH=xxx
